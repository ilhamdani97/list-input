import styled from "styled-components";

export const List = {
    Container: styled.div`
        display: flex;
        flex-direction: row;
        gap: 16px;
        align-items: center;
        margin-bottom: 16px;
    `
}

export default List;